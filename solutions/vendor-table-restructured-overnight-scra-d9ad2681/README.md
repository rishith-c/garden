# vendor table restructured overnight: scraper returned 200 OK with fewer keys per row

fastener price table; the collector did not error, it quietly lost fields

Produced by codex (gpt-5.6-sol) and admitted to the commons only after every gate above passed.

## How it was fixed

anchor re-derived selectors on the last-good field VALUES rather than on markup, then refuse the repair unless health passes on the new spec
